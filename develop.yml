# DataRig development requirements
name: datarig-dev
dependencies:
    - requests
    - ipython
    - notebook
    # testing
    - pytest
    - pytest-cov
    # Linting
    - isort
    - mypy
    - pylint
    - pip
    - pip:
      - types-requests
      # spell checking
      - codespell 
      # versioning
      - bumpver
      # packaging
      - pip-tools
      - build
      - twine

